node_modules/
.expo/
dist/
.turbo/
*.log
.env
.env.local
.DS_Store
ios/
android/

# Next.js build output
.next/
out/
*.tsbuildinfo

# IDEs
.idea/
.vscode/

# Xcode DerivedData from local iOS builds
apps/mobile/build/
.vercel
.env*
# ...but the committed templates must stay tracked (the Vercel CLI appended the
# blanket `.env*` above and would otherwise swallow them).
!.env.example
!**/.env.example
